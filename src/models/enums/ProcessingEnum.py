from enum import Enum 

class ProcessingEnum(Enum):

    # Text formats
    TXT = ".txt"
    MD = ".md"
    CSV = ".csv"
    JSON = ".json"

    # Documents
    PDF = ".pdf"
    DOCX = ".docx"

    # Web
    HTML = ".html"